using UnityEngine;

/// <summary>
/// Decorative room visuals (e.g. floor) that must not affect <see cref="DungeonRoomSpawner"/>'s
/// auto-balance footprint calculation.
/// </summary>
public class RoomFootprintIgnore : MonoBehaviour
{
}
