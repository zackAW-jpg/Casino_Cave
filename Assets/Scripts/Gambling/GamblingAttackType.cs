/// <summary>Left &amp; right slot symbols. Equal weight when rolling unless lists are customized.</summary>
public enum GamblingAttackType
{
    Punch,
    ShurikenThrow,
    ExtendedPunch,
    HammerSlam
}
